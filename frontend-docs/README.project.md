# Data Layer

During the MVP stage, FlashTown uses Google Sheets as a structured data source.

Sheets are modeled like database tables:
- shops
- offers
- categories

This approach allows rapid iteration before migrating to MongoDB.
